services:

  opendms_database:
    container_name: "opendms_database"
    image: ${image_mariadb}
    expose:
      - "3306"
    environment:
      MARIADB_USER: user
      MARIADB_PASSWORD: pa55w0rd
      MARIADB_ROOT_PASSWORD: R00tpa55w0rd
      MARIADB_DATABASE: OpenDMSDatabase
    volumes:
      - ./Volumes/OpenDMSMariaDBData:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mariadb-admin ping -h 127.0.0.1 -u root -pR00tpa55w0rd || exit 1"]
      interval: 2s
      timeout: 2s
      retries: 20
      start_period: 5s
    networks:
      - opendms_net

  opendms_database_administration:
    image: ${image_adminer}
    container_name: "opendms_database_administration"
    expose:
      - "8080"
    networks:
      - opendms_net

networks:
  opendms_net:
    external: true
