services:

  opendms_database:
    container_name: "opendms_database"
    image: ${image_mariadb}
    ports:
      - "3306:3306"
    environment:
      MARIADB_USER: user
      MARIADB_PASSWORD: pa55w0rd
      MARIADB_ROOT_PASSWORD: R00tpa55w0rd
      MARIADB_DATABASE: OpenDMSDatabase
    volumes:
      - ./Volumes/OpenDMSMariaDBData:/var/lib/mysql
    networks:
      - opendms_net

  opendms_database_administration:
    image: ${image_adminer}
    container_name: "opendms_database_administration"
    ports:
      - 8080:8080
    networks:
      - opendms_net

networks:
  opendms_net:
    external: true
