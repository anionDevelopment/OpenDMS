DROP DATABASE OpenDMSDatabase;
CREATE DATABASE OpenDMSDatabase CHARACTER SET utf8mb4;
-- dropping the current database leaves the connection without a default-database, so it has to be selected again.
USE OpenDMSDatabase;
